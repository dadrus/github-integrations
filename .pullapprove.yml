version: 2
groups:
  code-review:
    approve_by_comment:
      approve_regex: ^(Approved|:shipit:|:\+1:|:thumbsup:|LGTM|Ok|\+1)
      enabled: true
      reject_regex: ^(Rejected|:\-1:|:thumbsdown:|\-1)
    required: 1
    reset_on_push:
      enabled: true
    users:
    - dadrus
